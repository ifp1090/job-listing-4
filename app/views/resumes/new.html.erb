<h3> 投交简历到 <%= @job.title %> </h3>

<hr>

<%= simple_form_for [@job, @resume] do |f| %>
  <%= f.input :content, :label => "描述一下你自己" %>
  <%= f.input :attachment, :label => "上传简历" %>

  <%= f.submit "送出", class: "btn btn-success", data: { disable_with: "Submitting..." } %>
<% end %>
